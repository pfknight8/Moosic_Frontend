const SignUpForm = () => {
  return (
    <form id="signUpForm">
      <p>This is our form for registration. Consider what we need here; fields (input, select, etc), field labels, buttons, etc.</p>
    </form>
  )
}

export default SignUpForm