const LoginForm = () => {
  return (
    <form id="loginForm">
      <p>This is our login form. Consider what we need here; fields (input, select, etc), field labels, buttons, etc.</p>
    </form>
  )
}

export default LoginForm